{% set ssmtp_map = salt['grains.filter_by']({
  'default': {
    'config': {
      'Root': 'postmaster',
      'MailHub': 'mail',
    },
    'revaliases': {},
  },
},
merge=salt['pillar.get']('ssmtp:lookup', {}),
default='default') %}
